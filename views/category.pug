extends layout

block content
    h1= title
    
    if itemList.length
        ul.productList
            each itemName in itemList
                li
                    p #[strong Product: ] 
                        a(href='/category/item/' + itemName._id) #{itemName.name}
                    p.description #{itemName.description}
                    p #[strong Price: ] € #{itemName.price}
                    p #[strong Items in stock: ] #{itemName.numberInStock}
    else
        p There are no item
    hr
    p.create
        a(href="/") Home page (view categories)
    p.create
        a(href=category.url+'/delete') Delete category
    p.create
        a(href=category.url+'/update') Update category
    p.create
        a(href='/category/item/create') Create a new item